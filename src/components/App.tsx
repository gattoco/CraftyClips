import { Show, createSignal, onMount } from 'solid-js';
import { useState } from '../store';

const App = () => {
  const { state, setState } = useState();
 

  return (
    <div>
      Home dashboard something soemthing
    </div>
  );
};

export default App;
